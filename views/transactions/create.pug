extends ../layout/common
block content
  h1 Create new transaction
  form(action="/transactions/create", method="POST")

    select(name="userId")
      each user in users
        option= user.name

    select(name="bookId")
      each book in books
        option= book.title
    button Create
